fonts = {
  family: s('"Open Sans", sans-serif'),
  regular: 400,
  bold: 700
}

colors = {
  green: #00AE64
}

breakpoints = {
  small: '(max-width: 46.875em)', //700
  hidpi: '(min-device-pixel-ratio: 1.3), (min-resolution: 120dpi)',
}
